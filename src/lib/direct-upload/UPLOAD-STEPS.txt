DIRECT UPLOAD — Study Planner Pro (final, chat-anchor root fix)
================================================================
1. GitHub -> Add file -> Upload files
2. Drag in tsconfig.json AND the src folder
3. Commit to main -> Vercel deploys
4. If the deploy-package folder still exists in the repo, delete it
   (build passes either way; tsconfig excludes it)
5. After green deploy: hard refresh (Ctrl+Shift+R)

KEY FIX THIS BUILD: desktop chat was left-stuck because the hover
sheen rule set position:relative on all glass panels, overriding the
chat panel's position:fixed. Now excluded + re-asserted.
